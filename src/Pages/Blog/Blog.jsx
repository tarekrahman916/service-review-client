import React from "react";
import usePageTitle from "../../hooks/usePageTitle";

const Blog = () => {
  usePageTitle("Blogs");
  return (
    <div>
      <h2>Blog</h2>
    </div>
  );
};

export default Blog;
